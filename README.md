# Web-Scraping

import required packages
steps:
#1 set the url
#2 send request
#3 pass it through bs
#4 Extract the infobox table
#5 REad the first param
create an empty list
select the required tag
create a loop & append all the relevant tags as text
#6 Read the second tag
same as step5
#7 Create dataframe 
create an empty array.
loop & append the variables
#8 save the dataframe as csv file

















